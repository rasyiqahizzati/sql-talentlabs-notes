SELECT rating FROM ratings
WHERE movie_id IN (
    SELECT id FROM movies
    WHERE
    id IN (
        SELECT movie_id FROM years
        WHERE year > 2010
    
    )
    AND title LIKE "Toy Story%"
)